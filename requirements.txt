# Runtime
litellm>=1.0
rich>=13.0
pyfiglet>=1.0

# Provider SDK — install the one matching your model.
# Default demo uses Claude via ANTHROPIC_API_KEY:
anthropic>=0.20

# CSV parsing
chardet>=4.0
pandas>=2.0

# Dev / test
pytest>=7.0
